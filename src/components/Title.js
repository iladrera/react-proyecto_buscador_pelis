import React from "react";

// export const NombComponent: de esta manera obligamos a utilizar
// siempre el mismo nombre para usar este componente
export const Title = ({ children }) => <h1 className="title">{children}</h1>;
